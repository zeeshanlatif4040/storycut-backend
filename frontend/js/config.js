// Backend API base URL. Same-origin by default (works when Flask serves the UI).
// When hosting the frontend separately (e.g. Netlify), set this to your
// backend's public URL, e.g.:
//   window.STORYCUT_API_BASE = "https://storycut-api.example.com";
// Also set STORYCUT_CORS on the backend to your frontend's origin.
window.STORYCUT_API_BASE = "";
